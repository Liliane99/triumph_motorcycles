import { Section } from "@/components/Section"

export const Header = () => {  
    return (
        <header>
            <Section>
                Header
            </Section> 
        </header>
    )
}